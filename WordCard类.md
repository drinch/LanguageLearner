# WordCard类

[toc]

2025/01/28

## 简介

这是类似单词卡片的窗口，目前用于搜索界面显示搜索结果

窗口结构包含左侧的单词信息和右侧按钮，右侧的按钮目前负责删除功能，发送删除信号

## 具体功能

设定单词，显示单词，提供按钮进行操作，目前为删除操作

窗口布局，右侧为操作区，包含删除按钮，左侧为信息区，分上下两部分，上方为单词名，下方为释义信息（未实装）。同时提供窗口点击操作

成员函数：

```void WordCard::setWord(const Word &_word_)```：设定单词

```void WordCard::showWord()```：将单词显示在相应位置

```void mouseReleaseEvent(QMouseEvent *event)```：重载点击事件，发送点击信号。

信号：

```clicked()```：窗口被点击

```deleted()```：点击删除按钮

## 具体实现

暂无复杂功能